/**
 * Parses amount from text.
 * e.g., "2k" => -2000, "2rb" => -2000, "Rp5.000" => -5000
 */
export function parseAmount(text: string): number | null {
  // Implementation will be done in TASK-009
  return null;
}
