import { SuggestionMatch } from "./types";

/**
 * Looks up exact keywords and aliases.
 */
export function findExactMatch(text: string): SuggestionMatch | null {
  // To be implemented in TASK-019
  return null;
}
