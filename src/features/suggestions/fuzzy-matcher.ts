import { SuggestionMatch } from "./types";

/**
 * Uses fuse.js to find fuzzy matches for typos.
 */
export function findFuzzyMatch(text: string): SuggestionMatch | null {
  // To be implemented in TASK-020
  return null;
}
