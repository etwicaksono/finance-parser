/**
 * Schema barrel export.
 *
 * Import all table schemas here so they are:
 * - accessible via the `schema` object in the Drizzle client
 * - picked up by drizzle-kit for migrations
 *
 * Tables will be added in TASK-005.
 */

// Placeholder export to make this a valid TypeScript module.
// Remove this line when real table exports are added (TASK-005).
export const __placeholder = {} as const;

// Re-export all tables when they are created (TASK-005):
// export * from "./categories";
// export * from "./accounts";
// export * from "./keyword-mappings";
// export * from "./aliases";
// export * from "./duplicate-hashes";
